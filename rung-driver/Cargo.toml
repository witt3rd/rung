[package]
name = "rung-driver"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Configure a pool of principals and run a ladder over a subject"

[dependencies]
rung = { path = "../rung" }
rung-std = { path = "../rung-std" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"

[dev-dependencies]
serde_yaml = "0.9"
